# Pinning versions to match exactly what the models were trained with.
# This prevents InconsistentVersionWarning and ensures reproducible predictions.
streamlit==1.60.0
pandas
numpy
scikit-learn==1.5.2
xgboost==3.2.0
joblib
pytest
